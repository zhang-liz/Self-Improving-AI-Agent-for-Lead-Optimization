# Backend (server)
PORT=3000
CORS_ORIGIN=http://localhost:5173
SENTIMENT_PROVIDER=keyword
# Optional: enable LLM tool-use recommendations (get_lead_details, get_recent_interactions)
# OPENAI_API_KEY=sk-...
# OPENAI_RECOMMEND_MODEL=gpt-4o-mini
# Recommendation cache TTL in minutes (5–15); default 10
# RECOMMEND_CACHE_TTL_MIN=10

# Frontend (Vite) – optional; defaults to http://localhost:3000
# VITE_API_URL=http://localhost:3000
